pragma solidity ^0.8 ;

import "RafluxStorage.sol";

//this the main contract in the raffle
contract RaffluxMain is RafluxStorage {

//   //STRUCT
//   //struct of proposal
//   struct Proposal{

//   }
//   //MAPPING
//   // Mapping from user address to the list of staked tokens for that user
//   mapping(address => uint256[]) public stakedTokens;

//   // Mapping from token ID to the address of the user who listed it
//   mapping(uint256 => address) public stakedTokenOwners;

//   // Mapping of uint256 to the address of the user who
//   mapping(address => mapping(uint256 => uint256)) timeLeft;

//  //FUNCTIONS

//  //list the nft
//  function list(address _tokenAddress, uint _tokenId, uint _time){
//     //list the nft
//     RaffluxStorage.depositNft(_tokenAddress, _tokenId);
//  }

//  //timeleft
//  function timeLeft() public {

//  }

}